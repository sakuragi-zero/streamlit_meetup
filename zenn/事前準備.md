当日はこの事前準備が完了している前提で進めます。  
「⑤ Devcontainer接続」までをあらかじめ実施してください。  

### ① 必要なもの一覧

* PC（Windows または Mac）
* Google アカウント（Antigravity 用）
* Docker

## ② Antigravity のインストール

👉 **公式サイト**
[https://antigravity.google/](https://antigravity.google/)

* Antigravity のインストーラを公式サイトからダウンロードして、実行
* Antigravity を初回起動
* Antigravity のエージェントの動作モードを指定。エージェントが予想外の動作をしないように、Secure Mode を指定するのが推奨
* Google アカウントでサインイン
* ファイルを開いたり作成したりできるところまで確認する

---

## ③ Docker のインストール

### ■ Windows の方

👉 **Docker Desktop for Windows（公式）**
[https://docs.docker.com/desktop/install/windows-install/](https://docs.docker.com/desktop/install/windows-install/)

* Windows 10 / 11

⚠️ Windows で Docker を導入する方法は複数あります。そのうち WSL2 + Docker Desktop で導入する方法が環境に起因するトラブルが少ないです。WSL2 + Docker Desktop による導入の手順は、以下の参考記事を確認してください。

👉 参考記事（WSL2 + Docker Desktop）
[https://learn.microsoft.com/ja-jp/windows/wsl/tutorials/wsl-containers](https://learn.microsoft.com/ja-jp/windows/wsl/tutorials/wsl-containers)

---

### ■ Mac の方

👉 **Docker Desktop for Mac（公式）**
[https://docs.docker.com/desktop/install/mac-install/](https://docs.docker.com/desktop/install/mac-install/)

* Intel / Apple Silicon（M1/M2/M3）どちらも対応
* 基本的にはそのままインストールでOK

---

### ④ Docker が起動できるか確認

ターミナル（または PowerShell）で以下を実行：

```bash
docker --version
```

バージョンが表示されればOKです。

---

### ⑤ Devcontainer接続
- 作業するフォルダを作成
- Antigravityで作成したフォルダを開く
- .devcontainer/devcontainer.jsonファイルを作成
- command(control) + shift + Pで```reopen in contain```と入力
- 作成した.devcontainer.jsonを選択しエラーなくコンテナ環境に接続できることを確認する

```json
{
  "name": "Streamlit_meetup",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",
  "workspaceFolder": "/workspaces/{作業するフォルダ名}",
  "features": {
    "ghcr.io/devcontainers/features/node:1": {
      "version": "22"
    }
  },
  "postCreateCommand": "sh -c \"pip install uv streamlit && sudo apt-get update && sudo apt-get install -y chromium\"",
  "portsAttributes": {
    "5173": {
      "label": "Streamlit_meetup",
      "onAutoForward": "notify"
    }
  }
}
```