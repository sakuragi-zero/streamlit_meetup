{
    "name": "streamlit_meetup",
    "image": "mcr.microsoft.com/devcontainers/python:3.12",
    "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
    "features": {
        "ghcr.io/devcontainers/features/node:1": {
            "version": "22"
        }
    },
    "postCreateCommand": "sh -c \"pip install uv streamlit && pip install -r requirements.txt && sudo apt-get update && sudo apt-get install -y chromium fonts-noto-cjk fonts-noto-color-emoji\"",
    "portsAttributes": {
        "5173": {
            "label": "streamlit_meetup",
            "onAutoForward": "notify"
        }
    }
}